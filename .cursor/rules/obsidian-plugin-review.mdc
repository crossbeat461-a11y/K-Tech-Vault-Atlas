---
description: K-Tech Vault Atlas — コミュニティ審査 Warning 回避
globs:
  - "**/*"
alwaysApply: true
---

# Obsidian コミュニティ審査 — Vault Atlas

公開前に必ず確認する。

## manifest.json

- `description` に **Obsidian** を入れない
- `name` に **Plugin** を入れない（K-Tech Vault Atlas でよい）
- `authorUrl` は GitHub **プロファイル** URL（リポジトリ URL 不可）
- `fundingUrl` を設定（Buy Me a Coffee）
- `isDesktopOnly` は実態に合わせる（モバイルでもスキャン可なら false）

## コード

- **ネットワークリクエスト禁止**（スキャンは Vault 内のみ）
- 本番で `console.log` / `console.error` を出さない（`esbuild.config.mjs` の `drop: ["console", "debugger"]`）
- GitHub Release は tag = manifest.version。`main.js` / `manifest.json` / `styles.css` を **個別 asset** で添付（zip のみ不可）
- 破壊的操作は Phase 2 以降。Phase 1 は読み取りのみ
- `loadData` / `saveData` は `storage.ts` 経由。旧形式も `parseStorage` で吸収

## 設定 UI

- 設定は **タブ**（Profile / Scan / Support）
- `getSettingDefinitions()` を実装する（1.13 の設定検索）。`display()` は 1.8.7 向けフォールバック。`PluginSettingTab#update()` は 1.13 専用なので `minAppVersion` が 1.8.7 のときは呼ばない
- Support タブに Buy Me a Coffee ボタン
- 初回インストール・版更新時は `FundingModal`（`lastSeenVersion` で1回）
- `loadData()` は `unknown` に落とす。`JSON.parse` / `Array.isArray` も unknown 経由
- 削除は `FileManager.trashFile()`。設定フォルダは `Vault#configDir`（`.obsidian` を直書きしない）

## リリース資産

- リポジトリは **public**（private では attestation 不可）
- tag push → CI で `npm ci` / `npm run build` 後、**ファイルごとに** attestation → Release へ `main.js`, `manifest.json`, `styles.css`
- `main.js` / `styles.css` のバイト列は版ごとに変える（バナーに版番号）。同じハッシュを再リリースしない。Obsidian は digest 単位で古い attestation も見る
- **rerun しない**。失敗時は新しい版番号で tag を切る
- `LISTING.md` は掲載時にエージェントが貼る（200 / 1000 文字制限）
